services:
  - type: web
    name: focusflow-api
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: OPENAI_API_KEY
        sync: false

  - type: web
    name: focusflow-web
    runtime: static
    rootDir: .
    buildCommand: ""
    staticPublishPath: .
